<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE struts PUBLIC
        "-//Apache Software Foundation//DTD Struts Configuration 2.3//EN"
        "http://struts.apache.org/dtds/struts-2.3.dtd">

<struts>
    <constant name="struts.i18n.encoding" value="UTF-8" />
    <!-- 该属性指定需要Struts2处理的请求后缀，该属性的默认值是action，即所有匹配*.action的请求都由Struts2处理。 如果用户需要指定多个请求后缀，则多个后缀之间以英文逗号（,）隔开。 -->
    <constant name="struts.action.extension" value="do,action" />
    <!-- 设置浏览器是否缓存静态内容,默认值为true(生产环境下使用),开发阶段最好关闭 -->
    <constant name="struts.serve.static.browserCache" value="false" />
    <!-- 当struts的配置文件修改后,系统是否自动重新加载该文件,默认值为false(生产环境下使用),开发阶段最好打开 -->
    <constant name="struts.configuration.xml.reload" value="true" />
    <!-- 开发模式下使用,这样可以打印出更详细的错误信息 -->
    <constant name="struts.devMode" value="true" />
    <!-- 默认的视图主题 -->
    <!-- <constant name="struts.ui.theme" value="simple" /> -->
    <!-- 与spring集成，action由spring负责创建， -->
    <constant name="struts.objectFactory" value="spring" />

    <constant name="struts.convention.package.locators" value="action,actions,struts,web"/>
    <!-- <package name="test" namespace="config" extends="json-default"> -->
    <constant name="struts.multipart.maxSize" value="10701096"/>

    <!--<package name="default" extends="json-default">
       &lt;!&ndash; <default-action-ref name="notFound" />
        <action name="notFound">
            <result type="json">
                &lt;!&ndash;<param name="actionErrors"></param>&ndash;&gt;
                <param name="errorCode"/>
            </result>
        </action>&ndash;&gt;
        &lt;!&ndash;<interceptors>
            <interceptor name="myErrorInterceptor" class="com.mufan.utils.intercept.MyErrorInterceptor"/>
            <interceptor-stack name="myError">
                <interceptor-ref name="myErrorInterceptor"/>
                <interceptor-ref name="defaultStack"/>
            </interceptor-stack>
        </interceptors>
        <default-interceptor-ref name="myError"/>
        <global-results>
            <result name="error_500" type="json"><param name="root">jsonResult</param></result>
            <result name="500" type="json"><param name="root">jsonResult</param></result>
        </global-results>
        <global-exception-mappings>
            <exception-mapping result="error_500" exception="java.lang.Exception"/>
            <exception-mapping result="error_500" exception="com.mufan.utils.JsonHandlerException"/>
        </global-exception-mappings>&ndash;&gt;
        &lt;!&ndash;<default-action-ref name="notFound" />
        <action name="notFound">
            &lt;!&ndash;<result type="json">
               <param name="actionErrors">sssss</param>
                <param name="errorCode">500</param>
                <param name="statusCode">500</param>
            </result>&ndash;&gt;
            <result name="success" type="plainText" >
                <param name="location">/WEB-INF/errorpage/error_500.jsp</param>
                <param name="charSet">UTF-8</param>
            </result>
        </action>&ndash;&gt;
    </package>-->
</struts>